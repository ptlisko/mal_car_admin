import * as React from 'react';

import './styles.css';

const SellPage: React.FC = (): JSX.Element => {
    return (
        <div>
            SELL PAGE
        </div>
    );
};

export default SellPage;
