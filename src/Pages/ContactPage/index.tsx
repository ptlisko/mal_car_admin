import * as React from 'react';

import './styles.css';

const ContactPage: React.FC = (): JSX.Element => {
    return (
        <div>
            CONTACT PAGE
        </div>
    );
};

export default ContactPage;
