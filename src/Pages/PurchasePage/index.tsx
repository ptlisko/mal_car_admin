import * as React from 'react';

import './styles.css';

const PurchasePage: React.FC = (): JSX.Element => {
    return (
        <div>
            PURCHASE PAGE
        </div>
    );
};

export default PurchasePage;
